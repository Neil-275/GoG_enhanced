## Evaluation
Suppose prediction result to be placed at `./results/family.json` \
Run this command at the project's root to evaluate the result:
```bash
python3 evaluation.py --prediction_file ./results/family.json --output_file ./results/family_metrics.json